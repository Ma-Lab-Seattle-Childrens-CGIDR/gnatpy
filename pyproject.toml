[project]
name = "gnatpy"
version = "0.1.0"
description = "Analyze Rank Entropy in Python"
readme = "README.md"
authors = [
    { name = "Braden Griebel", email = "bgriebel@uw.edu" }
]
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["uv_build>=0.10.11,<0.11.0"]
build-backend = "uv_build"
